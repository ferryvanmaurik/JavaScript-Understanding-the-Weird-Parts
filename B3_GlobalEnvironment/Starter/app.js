// If your run 'this' variable in the browser, the window is the global object

var a = 'Hello Ferry'

function b() {
    // This fucntion is attached to the global object
    // You can check this in the developer tools
}